import React from "react";
import { createClient } from "@/supabase/server";

const page = async () => {
  
  
  return (
    <>
      <p className="w-full bg-primary">page</p>
    </>
  );
};

export default page;
